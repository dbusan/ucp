#ifndef LOGTEST_H
#define LOGTEST_H

/**
  Reads an entire logfile

	parameters: a string that contains the log filename
	returns 0 if file can't be opened
	returns 1 if successful

**/
int readlog( char[] );



#endif
